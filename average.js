const numbers = [13, 1334545, 13, 34633];
const size = numbers.length 
let sum = 0;
for(const number of numbers){
    sum = sum + number;
}
    const averageResult = sum / size;
    console.log(averageResult,'all numbers',);